opt_out_usage

desc("Install the shared Developer ID signing identity without changing Apple or Match state")
lane :sync_macos_signing do
  match(
    type: "developer_id",
    platform: "macos",
    readonly: true,
    skip_provisioning_profiles: true,
  )
end
